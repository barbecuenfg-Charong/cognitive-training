# 12-仓库清理记录：移除 numerology 子目录

- 日期：2026-03-04
- 类型：仓库结构清理（非功能需求）
- 执行人：维护任务自动化

## 背景

`numerology/` 目录是独立的 Next.js 脚手架项目（默认模板内容），与当前认知训练主应用无运行时引用关系，也未被主页导航、构建脚本、测试脚本使用。

保留该目录会造成以下问题：

1. 增加仓库体积和认知负担。
2. 让项目边界不清晰（误以为是主应用子模块）。
3. 提高维护和审查成本。

## 本次变更

已删除目录：

- `numerology/`

## 影响评估

1. 不影响主应用入口与运行方式（`npm run web`、`npm start`）。
2. 不影响训练模块页面、文档中心与测试目录。
3. 不影响现有发布结构（静态页面与 Electron 壳）。

## 验收检查

1. 仓库根目录不再存在 `numerology/`。
2. 仓库内无对 `numerology` 的外部引用。
3. 文档索引已记录本次清理动作。

## 回滚方式

若需恢复，请从历史提交中还原删除目录：

```bash
git checkout <commit_before_cleanup> -- numerology
```
